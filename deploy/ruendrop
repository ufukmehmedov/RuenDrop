#!/bin/sh
set -eu
if [ "$(id -u)" -ne 0 ]; then
    echo 'Run with sudo.' >&2
    exit 1
fi
set -a
. /etc/ruendrop/app.env
set +a
cd /opt/ruendrop
exec /opt/ruendrop/.venv/bin/python -m ruendrop.admin "$@"
